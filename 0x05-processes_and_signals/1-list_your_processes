#!/usr/bin/env bash
# Displays all running processes for all users, including those without a TTY,
# in a human-readable, hierarchical format with full start times.

ps axo user,pid,pcpu,pmem,vsz,rss,tty,stat,stime,time,command --forest \
  | sed '1s/STIME/STARTED/'
  