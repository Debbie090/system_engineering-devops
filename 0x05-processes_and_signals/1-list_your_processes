#!/usr/bin/env bash
# Displays all processes (all users, even those without a TTY)
# in a readable tree format with STARTED column and full process info

ps axo user,pid,pcpu,pmem,vsz,rss,tty,stat,start,time,command --forest
