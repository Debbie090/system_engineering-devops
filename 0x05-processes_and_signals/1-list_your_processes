#!/usr/bin/env bash
# Displays all processes (from all users, even without a TTY)
# in a readable process tree with a STARTED column in HH:MM:SS format.

ps axo user,pid,pcpu,pmem,vsz,rss,tty,stat,start,time,command --forest
