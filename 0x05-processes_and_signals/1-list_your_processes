#!/usr/bin/env bash
# Displays all running processes for all users, including those without a TTY, in a readable hierarchy format

ps -eo user,pid,pcpu,pmem,vsz,rss,tty,stat,start,time,command --sort=start_time | \
awk 'NR==1 {$6="STARTED"; print $0} NR>1 {print $0}'