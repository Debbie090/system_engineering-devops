#!/usr/bin/env bash
# Displays every process (all users, even those without a TTY),
# in a human‐readable, indented tree, with a seconds‐precision STARTED column.

ps axo user,pid,pcpu,pmem,vsz,rss,tty,stat,stime,time,command --forest \
  | sed '1s/STIME/STARTED/'
  