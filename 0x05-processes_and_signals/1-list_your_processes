#!/usr/bin/env bash
# Displays all processes (all users, even those without a TTY) 
# in a human-readable, hierarchical format with “STARTED” (HH:MM:SS).

ps axo user,stime,command --forest \
  | sed '1s/^USER *STIME/USER       STARTED/' 
  